<%= form_for(@uesertype) do |f| %>
  <% if @uesertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uesertype.errors.count, "error") %> prohibited this uesertype from being saved:</h2>

      <ul>
      <% @uesertype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
